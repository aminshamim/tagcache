# TagCache UI (MVP Skeleton)

React + Vite + TypeScript interface for TagCache.

## Scripts

- `pnpm dev` start dev server
- `pnpm build` production build
- `pnpm preview` preview build

Set `VITE_CACHE_API_BASE` environment variable.
